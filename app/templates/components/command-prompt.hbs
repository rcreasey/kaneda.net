<div {{bind-attr class=":prompt host:localhost"}}>
  <span>{{#if host}}{{ host }}{{ else }}kaneda.net{{/if}} %</span>
  <span {{bind-attr class="command:command:cursor"}}>{{ command }}{{yield}}&nbsp;</span>
  {{#if response}}
  <div class="response">{{response}}</div>
  {{/if}}
</div>
